# Pin the Robolectric runtime SDK to 34, decoupled from the app's targetSdk (36).
# Running Robolectric against SDK 36 requires JDK 21 (Android's SDK 36 android-all
# jar is Java-21 bytecode); the toolchain here is JDK 17, so tests run at 34.
sdk=34
